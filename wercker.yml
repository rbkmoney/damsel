box: rbkmoney/thrift-compiler
build:
    steps:
      - script:
        name: check syntactic correctness
        code: make all
    after-steps:
      - slack-notifier:
        url: ${SLACK_WEBHOOK_URL}
        username: "wercker"
